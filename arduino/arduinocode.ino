// Arduino sketch for capturing sensor data and sending via serial
/*

void setup() {
    Serial.begin(9600);
    // Set up sensor (e.g., radar, IMU, etc.)
  }
  
  void loop() {
    // Capture sensor data
    int sensorData = analogRead(A0); // example sensor data
  
    // Send sensor data to Jetson
    Serial.println(sensorData);
    delay(100); // Adjust the delay to control data rate
  }
  */